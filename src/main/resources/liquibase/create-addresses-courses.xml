<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">
    <changeSet id="create-add_course_seq" author="Ann" runOnChange="true">
        <createSequence cacheSize="1" cycle="false" sequenceName="add_course_seq" dataType="bigint" incrementBy="1" maxValue="9223372036854775807" minValue="1"  startValue="1"/>
    </changeSet>
    <changeSet id="create-addresses-courses-table" author="Ann" runOnChange="true">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="addresses_courses"/>
            </not>
        </preConditions>
        <sql>
            CREATE TABLE addresses_courses (
                address_id INT,
                course_id BIGINT,
                PRIMARY KEY (address_id, course_id),
                FOREIGN KEY (address_id) REFERENCES addresses(id),
                FOREIGN KEY (course_id) REFERENCES courses(id)
            );
        </sql>
    </changeSet>

    <changeSet id="update-addresses-courses-table" author="Ann" runOnChange="true">
        <preConditions onFail="MARK_RAN">
            <tableExists tableName="addresses_courses"/>
        </preConditions>
        <sql>
            ALTER TABLE addresses_courses DROP CONSTRAINT addresses_courses_pkey;

            ALTER TABLE addresses_courses ADD COLUMN id BIGINT NOT NULL DEFAULT nextval('add_course_seq');

            ALTER TABLE addresses_courses ADD PRIMARY KEY (id);
            ALTER TABLE addresses_courses ALTER COLUMN course_id TYPE BIGINT;
            ALTER TABLE addresses_courses DROP CONSTRAINT addresses_courses_course_id_fkey;

            ALTER TABLE addresses_courses ADD FOREIGN KEY (address_id) REFERENCES addresses(id);
            ALTER TABLE addresses_courses ADD FOREIGN KEY (course_id) REFERENCES courses(id);
        </sql>
    </changeSet>

</databaseChangeLog>