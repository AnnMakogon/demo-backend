<?xml version="1.0" encoding="UTF-8"?>
<migration>
    <changeSet id="1" author="Ann">
        <createTable tableName="students">
            <column name="id" type="bigint">
                <constraints primaryKey="true" nullable="false"/>
                <generatedValue class="org.hibernate.id.enhanced.SequenceStyleGenerator">
                    <parameters>
                        <parameter name="sequence_name" value="student_seq"/>
                    </parameters>
                </generatedValue>
            </column>
            <column name="fio" type="varchar(255)"/>
            <column name="group_id" type="bigint">
                <constraints foreignKeyName="fk_students_groups" references="groups(id)"/>
            </column>
            <column name="phone_number" type="varchar(255)"/>
            <column name="course_id" type="bigint">
                <constraints foreignKeyName="fk_students_courses" references="courses(id)"/>
            </column>
            <column name="department_id" type="bigint">
                <constraints foreignKeyName="fk_students_departments" references="departments(id)"/>
            </column>
            <column name="user_id" type="bigint">
                <constraints foreignKeyName="fk_students_users" references="users(id)"/>
            </column>
        </createTable>
    </changeSet>
</migration>